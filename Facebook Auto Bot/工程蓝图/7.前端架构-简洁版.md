# 7. 前端架构（简洁版）

## 技术栈概览

### 核心框架
- **React 19** + **TypeScript 5.5**：主框架，类型安全
- **Vite 5**：构建工具，快速开发
- **Ant Design 5**：UI组件库
- **React Router 7**：路由管理
- **Zustand**：状态管理
- **React Query**：数据获取和缓存

### PWA支持
- **Workbox**：Service Worker管理
- **Vite PWA Plugin**：PWA自动生成
- **IndexedDB**：离线数据存储

## 项目结构

```
src/
├── assets/           # 静态资源
├── components/       # 通用组件
│   ├── common/      # 按钮、输入框等
│   ├── layout/      # 布局组件
│   └── ui/          # UI组件
├── features/         # 功能模块
│   ├── auth/        # 认证
│   ├── accounts/    # 账号管理
│   ├── tasks/       # 任务管理
│   ├── scripts/     # 对话剧本
│   ├── monitoring/  # 监控
│   └── settings/    # 设置
├── hooks/           # 自定义Hook
├── stores/          # Zustand Store
├── services/        # API服务
├── utils/           # 工具函数
├── types/           # TypeScript类型
└── App.tsx          # 应用入口
```

## 核心功能模块

### 1. 认证模块
```typescript
// 功能：用户登录、注册、会话管理
// 组件：LoginForm, RegisterForm, AuthGuard
// Store：userStore (用户状态)
// API：authApi (认证接口)
```

### 2. 账号管理模块
```typescript
// 功能：10个Facebook账号管理
// 组件：AccountList, AccountForm, VPNConfig
// Store：accountStore (账号状态)
// API：accountApi (账号接口)
```

### 3. 任务管理模块
```typescript
// 功能：自动聊天任务创建和管理
// 组件：TaskList, TaskForm, ScheduleConfig
// Store：taskStore (任务状态)
// API：taskApi (任务接口)
```

### 4. 对话剧本模块
```typescript
// 功能：50个对话剧本浏览和选择
// 组件：ScriptBrowser, ScriptPreview, ScriptDetail
// Store：scriptStore (剧本状态)
// API：scriptApi (剧本接口)
```

### 5. 监控模块
```typescript
// 功能：实时任务监控和通知
// 组件：ExecutionMonitor, NotificationCenter, Dashboard
// Store：monitoringStore (监控状态)
// API：monitoringApi (监控接口)
```

## 状态管理设计

### Store结构
```typescript
// 用户Store
interface UserStore {
  user: User | null;
  isAuthenticated: boolean;
  login: (credentials) => Promise<void>;
  logout: () => void;
}

// 账号Store
interface AccountStore {
  accounts: FacebookAccount[];
  selectedAccount: FacebookAccount | null;
  loadAccounts: () => Promise<void>;
  createAccount: (data) => Promise<void>;
}

// 任务Store
interface TaskStore {
  tasks: TaskConfig[];
  activeTasks: TaskExecution[];
  createTask: (data) => Promise<void>;
  executeTask: (taskId) => Promise<void>;
}

// 通知Store
interface NotificationStore {
  notifications: Notification[];
  unreadCount: number;
  markAsRead: (id) => void;
  clearAll: () => void;
}
```

### 数据流
```
用户操作 → 组件 → 调用Store Action → 调用API服务
                                    ↓
API响应 → 更新Store状态 → 触发组件重渲染
```

## 路由设计

### 路由配置
```typescript
const routes = [
  // 公开路由
  { path: '/auth/login', element: <LoginPage /> },
  { path: '/auth/register', element: <RegisterPage /> },
  
  // 受保护路由（需要登录）
  {
    path: '/',
    element: <ProtectedRoute><MainLayout /></ProtectedRoute>,
    children: [
      { path: 'dashboard', element: <DashboardPage /> },
      { path: 'accounts', element: <AccountsPage /> },
      { path: 'accounts/:id', element: <AccountDetailPage /> },
      { path: 'tasks', element: <TasksPage /> },
      { path: 'tasks/new', element: <CreateTaskPage /> },
      { path: 'scripts', element: <ScriptsPage /> },
      { path: 'monitoring', element: <MonitoringPage /> },
      { path: 'settings', element: <SettingsPage /> },
    ],
  },
];
```

### 路由守卫
```typescript
const ProtectedRoute: React.FC = ({ children }) => {
  const { isAuthenticated, isLoading } = useUserStore();
  
  if (isLoading) return <LoadingScreen />;
  if (!isAuthenticated) return <Navigate to="/auth/login" />;
  
  return <>{children}</>;
};
```

## API集成

### API客户端配置
```typescript
const apiClient = axios.create({
  baseURL: import.meta.env.VITE_API_URL,
  timeout: 30000,
});

// 请求拦截器（添加Token）
apiClient.interceptors.request.use((config) => {
  const token = localStorage.getItem('access_token');
  if (token) config.headers.Authorization = `Bearer ${token}`;
  return config;
});

// 响应拦截器（处理错误）
apiClient.interceptors.response.use(
  (response) => response,
  (error) => {
    if (error.response?.status === 401) {
      localStorage.removeItem('access_token');
      window.location.href = '/auth/login';
    }
    return Promise.reject(error);
  }
);
```

### API服务示例
```typescript
export const taskApi = {
  // 获取任务列表
  getTasks: (params) => apiClient.get('/tasks', { params }),
  
  // 创建对话任务
  createConversationTask: (data) => 
    apiClient.post('/tasks/conversation', data),
  
  // 立即执行任务
  executeTask: (taskId, options) =>
    apiClient.post(`/tasks/${taskId}/execute`, options),
};
```

## PWA功能实现

### Service Worker配置
```javascript
// vite.config.ts
export default defineConfig({
  plugins: [
    VitePWA({
      registerType: 'autoUpdate',
      manifest: {
        name: 'Facebook Auto Bot',
        short_name: 'FBA Bot',
        theme_color: '#1890ff',
        icons: [
          { src: 'pwa-192x192.png', sizes: '192x192' },
          { src: 'pwa-512x512.png', sizes: '512x512' },
        ],
      },
    }),
  ],
});
```

### 离线功能
```typescript
// 离线检测
const useOffline = () => {
  const [isOffline, setIsOffline] = useState(!navigator.onLine);
  
  useEffect(() => {
    const updateStatus = () => setIsOffline(!navigator.onLine);
    window.addEventListener('online', updateStatus);
    window.addEventListener('offline', updateStatus);
    return () => {
      window.removeEventListener('online', updateStatus);
      window.removeEventListener('offline', updateStatus);
    };
  }, []);
  
  return isOffline;
};

// 离线数据存储
class OfflineStorage {
  static saveAccounts(accounts) {
    localStorage.setItem('offline_accounts', JSON.stringify(accounts));
  }
  
  static getAccounts() {
    const data = localStorage.getItem('offline_accounts');
    return data ? JSON.parse(data) : [];
  }
}
```

## 性能优化策略

### 1. 代码分割
```typescript
// 懒加载页面组件
const DashboardPage = lazy(() => import('./pages/DashboardPage'));
const AccountsPage = lazy(() => import('./pages/AccountsPage'));

// 使用Suspense
<Suspense fallback={<LoadingSpinner />}>
  <Routes>
    <Route path="/dashboard" element={<DashboardPage />} />
    <Route path="/accounts" element={<AccountsPage />} />
  </Routes>
</Suspense>
```

### 2. 图片优化
- 使用WebP格式
- 图片懒加载
- 响应式图片（srcset）

### 3. 渲染优化
- 使用React.memo避免不必要的重渲染
- 使用useMemo和useCallback缓存计算结果
- 虚拟列表处理长列表

### 4. 缓存策略
- API响应缓存（React Query）
- 图片资源缓存（Service Worker）
- 本地数据缓存（IndexedDB）

## 移动端适配

### 响应式设计
```css
/* 移动端优先的媒体查询 */
.container {
  padding: 16px;
}

@media (min-width: 768px) {
  .container {
    padding: 24px;
  }
}

@media (min-width: 1024px) {
  .container {
    padding: 32px;
  }
}
```

### 触摸优化
- 增大点击目标（最小44x44px）
- 避免hover状态在移动端
- 支持滑动手势

### 移动端特定功能
- 下拉刷新
- 无限滚动
- 离线模式
- 推送通知

## 测试策略

### 单元测试
```typescript
// 使用Jest + Testing Library
test('登录表单验证', () => {
  render(<LoginForm />);
  const emailInput = screen.getByLabelText('邮箱');
  fireEvent.change(emailInput, { target: { value: 'test@example.com' } });
  expect(emailInput.value).toBe('test@example.com');
});
```

### 集成测试
```typescript
// 测试用户登录流程
test('用户登录成功', async () => {
  render(<LoginPage />);
  
  // 填写表单
  userEvent.type(screen.getByLabelText('邮箱'), 'user@example.com');
  userEvent.type(screen.getByLabelText('密码'), 'password123');
  
  // 提交表单
  userEvent.click(screen.getByRole('button', { name: '登录' }));
  
  // 验证跳转
  await waitFor(() => {
    expect(window.location.pathname).toBe('/dashboard');
  });
});
```

### E2E测试
```typescript
// 使用Playwright
test('创建自动聊天任务', async ({ page }) => {
  await page.goto('/tasks/new');
  await page.selectOption('select[name="script"]', 'script_001');
  await page.click('button[type="submit"]');
  await expect(page.locator('.success-message')).toBeVisible();
});
```

## 部署配置

### 构建配置
```javascript
// vite.config.ts
export default defineConfig({
  build: {
    outDir: 'dist',
    sourcemap: false,
    rollupOptions: {
      output: {
        manualChunks: {
          vendor: ['react', 'react-dom'],
          ui: ['antd'],
          utils: ['axios', 'zustand'],
        },
      },
    },
  },
});
```

### 环境变量
```env
# .env.production
VITE_API_URL=https://api.fbautobot.com/v1
VITE_APP_NAME=Facebook Auto Bot
VITE_PUBLIC_URL=https://app.fbautobot.com
```

### CDN配置
- 静态资源上传到CDN
- 开启Gzip压缩
- 设置缓存头

## 监控和错误处理

### 错误边界
```typescript
class ErrorBoundary extends React.Component {
  state = { hasError: false };
  
  static getDerivedStateFromError() {
    return { hasError: true };
  }
  
  render() {
    if (this.state.hasError) {
      return <ErrorFallback />;
    }
    return this.props.children;
  }
}
```

### 性能监控
```typescript
// 使用Web Vitals
import { getCLS, getFID, getLCP } from 'web-vitals';

getCLS(console.log);
getFID(console.log);
getLCP(console.log);
```

### 错误上报
```typescript
// 错误上报服务
const errorReporter = {
  captureException(error, context) {
    // 发送到错误监控服务
    console.error('Error:', error, context);
  },
};

// 全局错误处理
window.addEventListener('error', (event) => {
  errorReporter.captureException(event.error, {
    type: 'window_error',
  });
});
```

---

**文档版本**: 1.0  
**创建日期**: 2026-04-12  
**最后更新**: 2026-04-12  
**负责人**: 前端架构师